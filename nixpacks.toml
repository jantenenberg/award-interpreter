# Force Python provider so the build has Python/pip (repo also has package.json → Node would win otherwise)
providers = ["python"]

[phases.setup]
nixPkgs = ["python311"]

[phases.install]
# python3 from Nix (setup phase); avoids relying on PATH or venv in install layer
cmds = ["python3 -m pip install -r backend/requirements.txt"]
