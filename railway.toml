[build]
builder = "nixpacks"

[deploy]
startCommand = "cd backend && python scripts/migrate_and_seed.py && /opt/venv/bin/uvicorn app.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "on_failure"